type BudgetEntry @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String!
  description: String
  amount: Float!
}

type Sensor @model @auth(rules: [{allow: public}]) {
  id: ID!
  humidity: Float
  temperature: Float
} 